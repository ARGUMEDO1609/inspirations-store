#!/usr/bin/env bash
set -euo pipefail

printf '== Backend CI (server/) ==\n'
(
  cd server
  bin/ci
)

printf '\n== Frontend lint (house/) ==\n'
(
  cd house
  npm ci
  npm run lint
)

printf '\n== Frontend build (house/) ==\n'
(
  cd house
  npm run build
)
