const { execSync } = require('child_process');
const activeAdminPath = execSync('bundle show activeadmin', { encoding: 'utf-8' }).trim().split(/\r?\n/).pop();
module.exports = require(`${activeAdminPath}/plugin.cjs`);
